import { atom } from "recoil";

export const showRSVPModal = atom({
  key: "showRSVPModal",
  default: false,
});


export const showGiftModal = atom({
    key: "showGiftModal",
    default: false,
  });